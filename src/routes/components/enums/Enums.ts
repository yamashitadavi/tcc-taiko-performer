export enum ToastType {
    info = 'alert-info',
    success = 'alert-success', 
    warning = 'alert-warning', 
    error = 'alert-error'
}

export enum SpinnerSize {
    xs = 'loading-xs',
    sm = 'loading-sm',
    md = 'loading-md',
    lg = 'loading-lg'
}